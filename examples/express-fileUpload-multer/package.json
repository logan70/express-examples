{
  "name": "express-fileupload-multer",
  "version": "1.0.0",
  "description": "Express 默认并不处理 HTTP 请求中的数据，对于普通请求体（JSON、二进制、字符串）数据，可以使用 `body-parser` 中间件。而文件上传（`multipart/form-data`请求），可以基于请求流处理，也可以使用 formidable 模块或 `Multer`中间件。",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "multer": "^1.4.2"
  }
}
